# Copy this file to .env and fill in your Cloudflare Worker endpoint.
# Example:
# AUDIOPLAYER_FEEDBACK_WORKER_URL=https://audio-player-feedback.<your-subdomain>.workers.dev/report
AUDIOPLAYER_FEEDBACK_WORKER_URL=

# Optional shared key header for the worker (if enabled server-side):
# AUDIOPLAYER_FEEDBACK_WORKER_KEY=
